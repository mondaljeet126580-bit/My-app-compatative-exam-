name: Delta MCP smoke test

on:
  workflow_dispatch:
  push:
    paths:
      - 'delta-mcp/**'
      - '.github/workflows/delta-mcp-smoke-test.yml'

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check official Delta MCP package
        run: uvx delta-exchange-mcp --help
